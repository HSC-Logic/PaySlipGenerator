import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { useAuth } from '../../features/auth/AuthProvider'
export function CloudShell() { const auth = useAuth(); const navigate = useNavigate(); return <div className="cloud-shell"><header><NavLink to="/" className="cloud-brand">Sliply</NavLink><nav aria-label="Cloud navigation"><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/payments">Payments</NavLink><NavLink to="/companies">Companies</NavLink><NavLink to="/recipients">Recipients</NavLink><NavLink to="/settings">Settings</NavLink></nav><div className="account-menu"><span title={auth.user?.email}>{auth.user?.email}</span><button className="button secondary" onClick={() => void auth.signOut().then(() => navigate('/'))}>Sign out</button></div></header><Outlet /></div> }
