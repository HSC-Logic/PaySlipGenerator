import { useAuth } from '../../features/auth/AuthProvider'
export function CloudSettingsPage(){const auth=useAuth();return <main className="cloud-page"><h1>Account settings</h1><section className="settings-card"><h2>Account</h2><p><strong>Email:</strong> {auth.user?.email}</p><p><strong>Plan:</strong> {auth.plan}</p><p>Plan changes are administrator-controlled. There is no client-side Pro switch.</p></section><section className="settings-card"><h2>Privacy</h2><p>Cloud companies, recipients, and payment snapshots are stored in the configured Supabase project. Sign out does not delete either cloud records or local browser data.</p><p>Archive records from their management pages. Full account/cloud-data deletion currently requires project administrator support.</p></section></main>}
